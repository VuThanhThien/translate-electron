#!/usr/bin/env node
/**
 * API keys are configured in-app (Setup / Settings) via macOS Keychain.
 * No build-time OPENAI_API_KEY is required.
 */
console.log('[build] API keys are configured in the app (no .env key required for packaging)')
